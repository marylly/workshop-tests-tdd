describe(`Payment method validator`, () => {

    beforeAll(() => {
    });

    afterEach(() => {
    });

    it(`should validate if the boleto object dueDate is valid`, () =>{});
    it(`should throw Exception if the boleto object dueDate NOT is valid`, () =>{});
});