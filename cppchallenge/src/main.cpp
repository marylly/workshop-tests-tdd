#include <iostream>
#include <Environment.hpp>

int main() {
    //OSSource *source = new OSSource();
    //Variables *variable = new Variables(source);
    std::cout << "The Project started" << std::endl;
}